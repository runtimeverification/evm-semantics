#!/usr/bin/env bash
set -euo pipefail
shopt -s extglob
if [[ "$#" -eq 0 ]]; then
    kevm-pyk --help
    kontrol --help
    exit 2
fi
case "$1" in
    @(compile|solc-to-k|foundry)*) kontrol "$@" ;;
    *) kevm-pyk "$@" ;;
esac

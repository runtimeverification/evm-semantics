load { "gas"  : 10000000
  // Query: always ((~ overflow) \/ eventually revert)
  // , "code" : UNSAFEADD(100        , 100) // always ((~ overflow) \/ eventually revert)
  // , "code" : UNSAFEADD(maxUInt256 , 100) // (eventually revert) /\ always ((~ overflow) \/ eventually revert)
  // , "code" :   SAFEADD(100        , 100) // always ((~ overflow) \/ eventually revert)
     , "code" :   SAFEADD(maxUInt256 , 100) // always ((~ overflow) \/ eventually revert)
     }

start
